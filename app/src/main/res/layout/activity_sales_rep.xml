<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_sales_rep"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/realName"
    android:fitsSystemWindows="true"
    tools:context="com.kfc.productcar.ui.person.SalesRepActivity">
    <include
        layout="@layout/activity_title_personin"
        android:id="@+id/sales_title"></include>
    <ListView
        android:layout_below="@id/sales_title"
        android:layout_width="match_parent"
        android:scrollbars="none"
        android:divider="@null"
        android:id="@+id/sales_listView"
        android:layout_height="match_parent"></ListView>

</RelativeLayout>
